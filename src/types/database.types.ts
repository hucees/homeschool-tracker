/**
 * Placeholder only.
 *
 * After the Supabase project is linked and migrations are applied, regenerate
 * this file from the real database with:
 *
 *   npm run db:types
 *
 * Keeping this placeholder lets the starter compile before the hosted project
 * exists and prevents us from pretending hand-written types are authoritative.
 */
export type Database = never;
